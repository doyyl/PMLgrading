# Backend (Node.js + Express)

This is a simple Express backend for your project.

## Getting Started

1. Install dependencies:
   ```sh
   cd backend
   npm install
   ```
2. Start the backend server:
   ```sh
   npm start
   ```

The backend will run on http://localhost:3001 by default.
